import { CategoryKeyPipe } from './category-key.pipe';

describe('CategoryKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
